# Data

The training dataset is intentionally generated locally with `python src/generate_data.py` so the repository does not depend on an external proprietary dataset.

The generated `credit_risk.csv` is synthetic and suitable only for demonstration, testing, and portfolio purposes.
